@model IEnumerable<Tactsoft.Core.Entities.State>

@{
    ViewData["Title"] = "State";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    select.form-control {
        display: inline;
        width: 200px;
        margin-left: 25px;
    }
</style>

<div class="content-wrapper">
    <div class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h3 class="m-0">@ViewData["Title"]</h3>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Dashboard">Dashboard</a></li>
                        <li class="breadcrumb-item active">@ViewData["Title"]</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between">
                        <h3 class="card-title">@ViewData["Title"] List</h3>
                        <a class="btn btn-outline-primary btn-sm" onclick="AddCountry()">
                            <i class="fa-regular fa-pen-to-square"></i> Add New
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <form method="post" asp-action="Index" asp-controller="State">
                        <div class="country-filter">
                            <select class="form-control" id="CountryId" name="CountryId" asp-items="ViewBag.CountryId" onchange="this.form.submit()">
                                <option value="">Select Country</option>
                            </select>
                            
                        </div>
                        
                    </form>
                   
                    <table class="table" id="stateTable">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.CountryId)
                                </th>
                                <th style="width:90px">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Country.Name)
                                    </td>
                                    <td style="width:60px;text-align:center">
                                        @Html.ActionLink("", "Edit", new { id = item.Id }, new { @class = "fa-regular fa-pen-to-square", @style = "color:green" }) |
                                        @Html.ActionLink("", "Details", new { id = item.Id }, new { @class = "fa-regular fa-eye", @style = "color:blue" }) |
                                        @Html.ActionLink("", "Delete", new { id = item.Id }, new { @class = "fa-regular fa-trash-can", @style = "color:red" })
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </section>
</div>



@*@section Scripts{
    <script>
        $("document").ready(function () {
            $('#stateTable').DataTable({
                "processing": true,
                "serverSide": true,
                "searching": true,
                "ajax": {
                    "url": "/State/LoadData",
                    "type": "POST",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "name", "name": "Name", "autoWidth": true },
                    { "data": "countryId", "name": "CountryId", "autoWidth": true },
                ]
            });  

            var table = $('#stateTable').DataTable();

            $("#stateTable_filter.dataTables_filter").append($("#CountryId"));

            var countryIndex = 0;
            $("#stateTable th").each(function (i) {
                if ($($(this)).html() == "countryId") {
                    countryIndex = i; return false;
                }
            });

            $.fn.dataTable.ext.search.push(
                function (settings, data, dataIndex) {
                    var selectedItem = $('#CountryId').val()
                    var country = data[countryIndex];
                    if (selectedItem === "" || country.includes(selectedItem)) {
                        return true;
                    }
                    return false;
                }
            );
            $("#CountryId").change(function (e) {
                table.draw();
            });
            table.draw();

        });
    </script>*@
}